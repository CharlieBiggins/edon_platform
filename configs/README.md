# Configurations

Configurations are immutable inputs once an experiment begins. Experiment
manifests pin their exact content hashes.

`gateway/production.example.json` freezes the Agent Gateway protocol versions
and safe defaults. It is a reference contract, not deployable credentials or a
production authorization.