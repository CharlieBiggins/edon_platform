# Provenance

Provenance records connect sources, transformations, datasets, models,
experiments, approvals, and claims. Git stores small registries and hashes;
artifact bytes may live in protected or external storage.