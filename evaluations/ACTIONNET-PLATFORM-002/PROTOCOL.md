# Protocol

The evaluation registers one canonical Institutional IR with immutable object
identities, explicit tuple mappings, three time horizons, critical unknown
evidence, and one assumption-driven actor model. It registers three executable
mechanisms and two composition edges, executes and replays the composition,
records the resulting trajectory, creates and custodies two replay-verified
counterfactual experiences, samples a deterministic human-behavior scenario,
and accepts one locally minimized real-world abstraction while rejecting a
record containing a direct-identifier field.

All 26 gates must pass. The fixtures are internal, synthetic, and deliberately
bounded. No external production or scientific-transfer claim is authorized.