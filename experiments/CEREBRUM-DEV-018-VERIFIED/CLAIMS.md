# Claims

The completed run supports a project-authored synthetic verified-hybrid signal:
33/33 data controls and 24/24 core preflight controls passed; the frozen model
was accepted on 209/256 cases; the verifier overrode 47 cases and blocked 13
unsafe model-decision paths; the hybrid passed all 26 confirmation checks with
zero unsafe authorizations.

The deterministic executor is responsible for correctness whenever the model
is overridden. Model-only confirmation failed four checks and made four unsafe
authorizations. Acceptance rate quantifies, but does not inflate, the model's
contribution.

No outcome authorizes independent transfer, production deployment, autonomous
or binding authority, or an IGI claim.