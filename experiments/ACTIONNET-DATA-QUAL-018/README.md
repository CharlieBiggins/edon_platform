# ACTIONNET-DATA-QUAL-018

This campaign generates 64 fresh development cases and a sealed 192-case
confirmation for the DEV-018 verified-hybrid integration. It has no training
split. The deterministic verifier is required to operate from each record's
`input` object only; targets remain scorer-only data.

The qualified data can test an integration architecture. It cannot establish
that the language model learned the deterministic execution policy.