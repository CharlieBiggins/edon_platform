# Scaling architecture

Generation remains deterministic and bounded. The larger validation instrument
reduces the fragility of a mechanism floor based on only three appeal cases.
Future successors may expand institutions or renderers, but they must allocate
new lineages and cannot train on this validation set after exposure.