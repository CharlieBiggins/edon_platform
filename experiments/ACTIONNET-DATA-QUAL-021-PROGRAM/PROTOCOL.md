# Protocol

1. Generate split-disjoint fresh counterfactual trajectories from unused
   family identifiers.
2. Render only the heldout-validated `FAMILIAR_AUGMENTED` interface.
3. Compile a canonical temporal program containing ordered `STEP` statements,
   the executed final state, and the state-derived certificate.
4. Parse and execute every target through two independent implementations.
5. Require exact event coverage, order, boundary partition, state, certificate,
   lineage, and split-isolation controls.
6. Keep confirmation single-use and prohibit transfer or binding authority.