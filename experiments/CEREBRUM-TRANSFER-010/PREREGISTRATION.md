# CEREBRUM-TRANSFER-010 preregistration shell

Protocol identity: CEREBRUM-TRANSFER-010-v1.0.0

Frozen: September 4, 2026, before DEV-010 scoring and before any protected
instrument materialization.

## Question

If both frozen CEREBRUM-DEV-010 seeds pass their registered development gate,
do they preserve safe certificate, transition, canonical queue, decision-clock
deferral, state-reconstruction, causal-pair, and appeal behavior on a fresh,
independently authored and independently custodied synthetic EventNet
instrument?

## Candidate conditions

- DEV-010 seed 26090401;
- DEV-010 seed 26090402.

Candidate hashes remain blank until the frozen DEV-010 two-seed summary passes.
Candidate training must end before any protected generator seed, case, prompt,
label, or exact renderer identity exists.

## Reserved instrument

The protected instrument reserves 192 label-free records from 48
counterfactual pairs:

- 48 certificates;
- 48 transitions;
- 48 queue traces;
- 48 pair contrasts.

Four governance forms receive 48 records and 12 pairs each. Within every form,
eight pairs are pivotal, two are invariance pairs, and two are contextual
pairs. The forms must differ in authority topology, escalation semantics,
workflow shape, resource structure, and failure mode while remaining
representable in the frozen IR:

1. hierarchical and matrix regional operations;
2. incident-command restoration operations;
3. quorum and multiscope consortium governance;
4. jurisdictional permit and resource-allocation operations.

Exact names, mechanisms, renderers, timing profiles, seeds, cases, and labels
remain unmaterialized.

## Independence

- Protected instrument authors may not have authored ActionNet-009,
  ActionNet-010, DEV-009, DEV-010, RB1 validation, or their prompts.
- The protected generator and oracle must be separately implemented without
  importing ActionNet-001 through ActionNet-010 code or generated artifacts.
- The custodian must be distinct from candidate development and protected
  instrument authorship.
- Cases, prompts, pairs, domains, governance forms, renderers, families,
  timing profiles, predictions, and labels must have zero prohibited overlap.

## Baselines

The study registers:

- raw unmodified Qwen as a schema-sensitive diagnostic;
- interface-matched Qwen;
- prompted interface-matched Qwen;
- two strong frozen general-model baselines from different providers;
- the RB1 predecessor pair as an optional repair-effect diagnostic; and
- a transparent deterministic rules ceiling.

The primary learned comparator is the strongest eligible interface-matched
learned baseline. Candidate advancement requires at least 0.10 absolute gain
on the registered execution composite as well as every absolute safety and
task gate.

## Adaptation prohibition

After protected materialization, no condition may receive training, gradient
updates, calibration, retrieval changes, demonstrations containing instrument
content, prompt revision, parser patch, output repair, retry-budget change,
threshold tuning, or case-specific analysis before the single score.

## Scope exclusions

Raw-source compilation, compiler fidelity, gold-IR reasoning,
candidate-compiled-IR reasoning, explicit UNREPRESENTABLE output, persistent
memory, planning, action selection, and learned closed-loop operation are not
part of this transfer claim. They require separately developed candidates and
protocol identities.

## Interpretation

A pass supports only reproducible bounded synthetic EventNet transfer across
independent authorship, generator, renderer, and governance-form boundaries. It
does not establish source-grounded transfer, real-institution validity,
closed-loop competence, production safety, autonomous authority, IFM, or
mini-IGI.