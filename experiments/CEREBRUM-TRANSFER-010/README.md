# CEREBRUM-TRANSFER-010

This package preregisters the fresh transfer successor that may evaluate the
two CEREBRUM-DEV-010 repair adapters. It is a blocked protocol shell, not an
instrument or transfer result.

The reserved study is deliberately narrow. It tests certificate, transition,
queue-execution, state-reconstruction, causal-pair, and appeal behavior on
independently authored synthetic EventNet cases that remain expressible in the
existing Institutional IR. It does not test raw-source compilation,
gold-IR-versus-compiled-IR reasoning, explicit UNREPRESENTABLE output, learned
closed-loop operation, or mini-IGI.

Current status:

RESERVATION_CLOSED_UNMATERIALIZED_DEV010_FAILED

The operator-reported September 5, 2026 DEV-010 summary failed its frozen
two-seed prerequisite. This identity therefore remains unmaterialized and
cannot accept DEV-011 continued adapters. A later confirmed repair requires a
new transfer protocol identity and fresh independent commitments.

The original manifest and readiness report retain their pre-score shell status
for reproducibility. `results/closure-2026-09-05.json` records the subsequent
operational closure.

The shell contains no protected cases, prompts, labels, instrument seed,
generator, oracle, runtime, predictions, scorer, or score. Materialization is
forbidden until:

1. both DEV-010 seeds pass their complete frozen gates;
2. candidate adapters and manifests are hash-frozen;
3. interface-matched Qwen and two-provider frontier baselines are frozen;
4. independent authorship, generator, oracle, renderer, overlap, power, and
   custody commitments are supplied; and
5. authorize_materialization.py succeeds.

Run:

    python preflight.py
    python -m unittest discover -s tests -v

A passing shell preflight is infrastructure evidence only.