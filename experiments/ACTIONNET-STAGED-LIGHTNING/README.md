# ActionNet staged Lightning launcher

`preflight.py` runs the four CPU-only framework preflights. It downloads no model,
starts no GPU, registers no experiment and materializes no protected transfer
instrument. Follow the bundle's `ACTIONNET_STAGED_LIGHTNING_START_HERE.md` for
the gated sequence.