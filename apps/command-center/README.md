# Command Center frontend boundary

The executable frontend is maintained in `command-center/` in this prototype checkout. It may import shared contracts or generated API client types only. It must not import `packages/platform-core` or `packages/platform-testkit`.
